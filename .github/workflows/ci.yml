name: Train and Deploy Model

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  train-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Conda
      uses: conda-incubator/setup-miniconda@v2
      with:
        auto-update-conda: true
        activate-environment: msml-env
        environment-file: MLProject/conda.yaml

    - name: Install MLflow
      run: pip install mlflow

    - name: Run MLflow project
      run: |
        conda run -n msml-env mlflow run MLProject -P n_neighbors=3

    - name: Check if trained model exists
      run: |
        if [ ! -f MLProject/trained_model.pkl ]; then
          echo "Model file not found!"
          exit 1
        fi

    - name: Upload trained model artifact
      uses: actions/upload-artifact@v2  # Version reverted to v2
      with:
        name: trained-model
        path: MLProject/trained_model.pkl

    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build Docker image
      run: |
        docker build -t ${{ secrets.DOCKER_USERNAME }}/ml-model:latest ./MLProject

    - name: Push Docker image
      run: |
        docker push ${{ secrets.DOCKER_USERNAME }}/ml-model:latest
